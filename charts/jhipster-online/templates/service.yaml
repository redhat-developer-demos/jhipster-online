apiVersion: v1
kind: Service
metadata:
  name: {{ include "jhipster-online.name" . }}
  labels:
    app.kubernetes.io/name: {{ include "jhipster-online.name" . }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app.kubernetes.io/name: {{ include "jhipster-online.name" . }}
