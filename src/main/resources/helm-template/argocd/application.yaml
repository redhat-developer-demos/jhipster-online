apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: __APP_NAME__
  namespace: __ARGOCD_APP_NAMESPACE__
  labels:
    app.kubernetes.io/managed-by: jhipster-online
    app.kubernetes.io/instance: __APP_NAME__
spec:
  project: default
  source:
    repoURL: __GIT_REPO_URL__
    targetRevision: HEAD
    path: helm
    helm:
      valueFiles:
        - values.yaml
      # values.yaml in Git keeps __TARGET_NAMESPACE__ until deploy; Argo must override image.namespace
      # so internal registry paths resolve (same token replaced in OpenShiftDeploymentService.argoCDDeploy).
      parameters:
        - name: image.namespace
          value: __TARGET_NAMESPACE__
  destination:
    server: https://kubernetes.default.svc
    namespace: __TARGET_NAMESPACE__
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
