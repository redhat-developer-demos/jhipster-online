apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: '{{ .Values.app.name }}-workspace'
  labels:
    app.kubernetes.io/managed-by: jhipster-online
    app.kubernetes.io/instance: '{{ .Values.app.name }}'
    app.kubernetes.io/part-of: '{{ .Values.app.name }}'
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: '{{ .Values.app.name }}-dotnet'
  labels:
    app.kubernetes.io/version: '0.4'
    app.kubernetes.io/managed-by: jhipster-online
    app.kubernetes.io/instance: '{{ .Values.app.name }}'
    app.kubernetes.io/part-of: '{{ .Values.app.name }}'
  annotations:
    tekton.dev/pipelines.minVersion: '0.50.0'
    tekton.dev/categories: Build Tools
    tekton.dev/tags: dotnet
    tekton.dev/platforms: 'linux/amd64'
spec:
  description: >-
    dotnet publish for JHipster .NET Core apps (output under ./publish for OpenShift binary build).
  workspaces:
    - name: source
      description: Repository checkout.
    - name: maven-settings
      description: Unused (kept for Pipeline workspace compatibility with spring layout).
      optional: true
    - name: maven-local-repo
      description: Unused.
      optional: true
  params:
    - name: DOTNET_IMAGE
      type: string
      description: .NET SDK image (Red Hat catalog)
      default: registry.access.redhat.com/ubi8/dotnet-80:8.0
    - name: CONTEXT_DIR
      type: string
      description: Directory containing the .sln or .csproj
      default: '.'
  results:
    - description: Placeholder group id for pipeline compatibility
      name: group-id
      type: string
    - description: Placeholder artifact id
      name: artifact-id
      type: string
    - description: Placeholder version
      name: version
      type: string
  steps:
    - name: dotnet-publish
      image: $(params.DOTNET_IMAGE)
      workingDir: $(workspaces.source.path)/$(params.CONTEXT_DIR)
      script: |
        #!/usr/bin/env bash
        set -euo pipefail
        dotnet --info
        PUBLISH_OUT="$(workspaces.source.path)/$(params.CONTEXT_DIR)/publish"
        mkdir -p "$PUBLISH_OUT"
        SLN=$(find . -maxdepth 4 -name '*.sln' 2>/dev/null | head -1)
        if [ -n "$SLN" ]; then
          echo "Publishing solution: $SLN"
          dotnet publish "$SLN" -c Release -o "$PUBLISH_OUT"
        else
          CSPROJ=$(find . -maxdepth 5 -name '*.csproj' 2>/dev/null | grep -viE 'Test|Tests' | head -1)
          if [ -z "$CSPROJ" ]; then
            echo "ERROR: No .sln or .csproj found under $(pwd)" >&2
            exit 1
          fi
          echo "Publishing project: $CSPROJ"
          dotnet publish "$CSPROJ" -c Release -o "$PUBLISH_OUT"
        fi
        echo -n dotnet > $(results.group-id.path)
        echo -n app > $(results.artifact-id.path)
        echo -n 0.0.1 > $(results.version.path)
---
apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: '{{ .Values.app.name }}-openshift-client'
  labels:
    app.kubernetes.io/version: '0.2'
    app.kubernetes.io/managed-by: jhipster-online
    app.kubernetes.io/instance: '{{ .Values.app.name }}'
    app.kubernetes.io/part-of: '{{ .Values.app.name }}'
  annotations:
    tekton.dev/categories: Openshift
    tekton.dev/pipelines.minVersion: '0.17.0'
    tekton.dev/tags: cli
    tekton.dev/displayName: 'openshift client'
    tekton.dev/platforms: 'linux/amd64'
spec:
  workspaces:
    - name: manifest-dir
      optional: true
      description: >-
        The workspace which contains kubernetes manifests which we want to apply on the cluster.
    - name: kubeconfig-dir
      optional: true
      description: >-
        The workspace which contains the the kubeconfig file if in case we want to run the oc command on another cluster.
  description: >-
    This task runs commands against the cluster provided by user
    and if not provided then where the Task is being executed.

    OpenShift is a Kubernetes distribution from Red Hat which provides oc,
    the OpenShift CLI that complements kubectl for simplifying deployment
    and configuration applications on OpenShift.

  params:
    - name: SCRIPT
      description: The OpenShift CLI arguments to run
      type: string
      default: 'oc help'
    - name: VERSION
      description: The OpenShift Version to use
      type: string
      default: '4.7'
  steps:
    - name: oc
      image: quay.io/openshift/origin-cli:$(params.VERSION)
      script: |
        #!/usr/bin/env bash

        [[ "$(workspaces.manifest-dir.bound)" == "true" ]] && \
        cd $(workspaces.manifest-dir.path)

        [[ "$(workspaces.kubeconfig-dir.bound)" == "true" ]] && \
        [[ -f $(workspaces.kubeconfig-dir.path)/kubeconfig ]] && \
        export KUBECONFIG=$(workspaces.kubeconfig-dir.path)/kubeconfig

        $(params.SCRIPT)
---
apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: '{{ .Values.app.name }}-git-clone'
  labels:
    app.kubernetes.io/version: '0.9'
    app.kubernetes.io/managed-by: jhipster-online
    app.kubernetes.io/instance: '{{ .Values.app.name }}'
    app.kubernetes.io/part-of: '{{ .Values.app.name }}'
  annotations:
    tekton.dev/pipelines.minVersion: '0.38.0'
    tekton.dev/categories: Git
    tekton.dev/tags: git
    tekton.dev/displayName: 'git clone'
    tekton.dev/platforms: 'linux/amd64,linux/s390x,linux/ppc64le,linux/arm64'
    tekton.dev/deprecated: 'true'
spec:
  description: >-
    These Tasks are Git tasks to work with repositories used by other tasks
    in your Pipeline.

    The git-clone Task will clone a repo from the provided url into the
    output Workspace. By default the repo will be cloned into the root of
    your Workspace. You can clone into a subdirectory by setting this Task's
    subdirectory param. This Task also supports sparse checkouts. To perform
    a sparse checkout, pass a list of comma separated directory patterns to
    this Task's sparseCheckoutDirectories param.
  workspaces:
    - name: output
      description: The git repo will be cloned onto the volume backing this Workspace.
    - name: ssh-directory
      optional: true
      description: |
        A .ssh directory with private key, known_hosts, config, etc. Copied to
        the user's home before git commands are executed. Used to authenticate
        with the git remote when performing the clone. Binding a Secret to this
        Workspace is strongly recommended over other volume types.
    - name: basic-auth
      optional: true
      description: |
        A Workspace containing a .gitconfig and .git-credentials file. These
        will be copied to the user's home before any git commands are run. Any
        other files in this Workspace are ignored. It is strongly recommended
        to use ssh-directory over basic-auth whenever possible and to bind a
        Secret to this Workspace over other volume types.
    - name: ssl-ca-directory
      optional: true
      description: |
        A workspace containing CA certificates, this will be used by Git to
        verify the peer with when fetching or pushing over HTTPS.
  params:
    - name: url
      description: Repository URL to clone from.
      type: string
    - name: revision
      description: Revision to checkout. (branch, tag, sha, ref, etc...)
      type: string
      default: ''
    - name: refspec
      description: Refspec to fetch before checking out revision.
      default: ''
    - name: submodules
      description: Initialize and fetch git submodules.
      type: string
      default: 'true'
    - name: depth
      description: Perform a shallow clone, fetching only the most recent N commits.
      type: string
      default: '1'
    - name: sslVerify
      description: Set the `http.sslVerify` global git config. Setting this to `false` is not advised unless you are sure that you trust your git remote.
      type: string
      default: 'true'
    - name: crtFileName
      description: file name of mounted crt using ssl-ca-directory workspace. default value is ca-bundle.crt.
      type: string
      default: 'ca-bundle.crt'
    - name: subdirectory
      description: Subdirectory inside the `output` Workspace to clone the repo into.
      type: string
      default: ''
    - name: sparseCheckoutDirectories
      description: Define the directory patterns to match or exclude when performing a sparse checkout.
      type: string
      default: ''
    - name: deleteExisting
      description: Clean out the contents of the destination directory if it already exists before cloning.
      type: string
      default: 'true'
    - name: httpProxy
      description: HTTP proxy server for non-SSL requests.
      type: string
      default: ''
    - name: httpsProxy
      description: HTTPS proxy server for SSL requests.
      type: string
      default: ''
    - name: noProxy
      description: Opt out of proxying HTTP/HTTPS requests.
      type: string
      default: ''
    - name: verbose
      description: Log the commands that are executed during `git-clone`'s operation.
      type: string
      default: 'true'
    - name: gitInitImage
      description: The image providing the git-init binary that this Task runs.
      type: string
      default: 'ghcr.io/tektoncd/github.com/tektoncd/pipeline/cmd/git-init:v0.40.2'
    - name: userHome
      description: |
        Absolute path to the user's home directory.
      type: string
      default: '/home/git'
  results:
    - name: commit
      description: The precise commit SHA that was fetched by this Task.
    - name: url
      description: The precise URL that was fetched by this Task.
    - name: committer-date
      description: The epoch timestamp of the commit that was fetched by this Task.
  steps:
    - name: clone
      image: '$(params.gitInitImage)'
      env:
        - name: HOME
          value: '$(params.userHome)'
        - name: PARAM_URL
          value: $(params.url)
        - name: PARAM_REVISION
          value: $(params.revision)
        - name: PARAM_REFSPEC
          value: $(params.refspec)
        - name: PARAM_SUBMODULES
          value: $(params.submodules)
        - name: PARAM_DEPTH
          value: $(params.depth)
        - name: PARAM_SSL_VERIFY
          value: $(params.sslVerify)
        - name: PARAM_CRT_FILENAME
          value: $(params.crtFileName)
        - name: PARAM_SUBDIRECTORY
          value: $(params.subdirectory)
        - name: PARAM_DELETE_EXISTING
          value: $(params.deleteExisting)
        - name: PARAM_HTTP_PROXY
          value: $(params.httpProxy)
        - name: PARAM_HTTPS_PROXY
          value: $(params.httpsProxy)
        - name: PARAM_NO_PROXY
          value: $(params.noProxy)
        - name: PARAM_VERBOSE
          value: $(params.verbose)
        - name: PARAM_SPARSE_CHECKOUT_DIRECTORIES
          value: $(params.sparseCheckoutDirectories)
        - name: PARAM_USER_HOME
          value: $(params.userHome)
        - name: WORKSPACE_OUTPUT_PATH
          value: $(workspaces.output.path)
        - name: WORKSPACE_SSH_DIRECTORY_BOUND
          value: $(workspaces.ssh-directory.bound)
        - name: WORKSPACE_SSH_DIRECTORY_PATH
          value: $(workspaces.ssh-directory.path)
        - name: WORKSPACE_BASIC_AUTH_DIRECTORY_BOUND
          value: $(workspaces.basic-auth.bound)
        - name: WORKSPACE_BASIC_AUTH_DIRECTORY_PATH
          value: $(workspaces.basic-auth.path)
        - name: WORKSPACE_SSL_CA_DIRECTORY_BOUND
          value: $(workspaces.ssl-ca-directory.bound)
        - name: WORKSPACE_SSL_CA_DIRECTORY_PATH
          value: $(workspaces.ssl-ca-directory.path)
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
      script: |
        #!/usr/bin/env sh
        set -eu

        if [ "${PARAM_VERBOSE}" = "true" ] ; then
          set -x
        fi

        if [ "${WORKSPACE_BASIC_AUTH_DIRECTORY_BOUND}" = "true" ] ; then
          cp "${WORKSPACE_BASIC_AUTH_DIRECTORY_PATH}/.git-credentials" "${PARAM_USER_HOME}/.git-credentials"
          cp "${WORKSPACE_BASIC_AUTH_DIRECTORY_PATH}/.gitconfig" "${PARAM_USER_HOME}/.gitconfig"
          chmod 400 "${PARAM_USER_HOME}/.git-credentials"
          chmod 400 "${PARAM_USER_HOME}/.gitconfig"
        fi

        if [ "${WORKSPACE_SSH_DIRECTORY_BOUND}" = "true" ] ; then
          cp -R "${WORKSPACE_SSH_DIRECTORY_PATH}" "${PARAM_USER_HOME}"/.ssh
          chmod 700 "${PARAM_USER_HOME}"/.ssh
          chmod -R 400 "${PARAM_USER_HOME}"/.ssh/*
        fi

        if [ "${WORKSPACE_SSL_CA_DIRECTORY_BOUND}" = "true" ] ; then
           export GIT_SSL_CAPATH="${WORKSPACE_SSL_CA_DIRECTORY_PATH}"
           if [ "${PARAM_CRT_FILENAME}" != "" ] ; then
              export GIT_SSL_CAINFO="${WORKSPACE_SSL_CA_DIRECTORY_PATH}/${PARAM_CRT_FILENAME}"
           fi
        fi
        CHECKOUT_DIR="${WORKSPACE_OUTPUT_PATH}/${PARAM_SUBDIRECTORY}"

        cleandir() {
          # Delete any existing contents of the repo directory if it exists.
          #
          # We don't just "rm -rf ${CHECKOUT_DIR}" because ${CHECKOUT_DIR} might be "/"
          # or the root of a mounted volume.
          if [ -d "${CHECKOUT_DIR}" ] ; then
            # Delete non-hidden files and directories
            rm -rf "${CHECKOUT_DIR:?}"/*
            # Delete files and directories starting with . but excluding ..
            rm -rf "${CHECKOUT_DIR}"/.[!.]*
            # Delete files and directories starting with .. plus any other character
            rm -rf "${CHECKOUT_DIR}"/..?*
          fi
        }

        if [ "${PARAM_DELETE_EXISTING}" = "true" ] ; then
          cleandir || true
        fi

        test -z "${PARAM_HTTP_PROXY}" || export HTTP_PROXY="${PARAM_HTTP_PROXY}"
        test -z "${PARAM_HTTPS_PROXY}" || export HTTPS_PROXY="${PARAM_HTTPS_PROXY}"
        test -z "${PARAM_NO_PROXY}" || export NO_PROXY="${PARAM_NO_PROXY}"

        git config --global --add safe.directory "${WORKSPACE_OUTPUT_PATH}"
        /ko-app/git-init \
          -url="${PARAM_URL}" \
          -revision="${PARAM_REVISION}" \
          -refspec="${PARAM_REFSPEC}" \
          -path="${CHECKOUT_DIR}" \
          -sslVerify="${PARAM_SSL_VERIFY}" \
          -submodules="${PARAM_SUBMODULES}" \
          -depth="${PARAM_DEPTH}" \
          -sparseCheckoutDirectories="${PARAM_SPARSE_CHECKOUT_DIRECTORIES}"
        cd "${CHECKOUT_DIR}"
        RESULT_SHA="$(git rev-parse HEAD)"
        EXIT_CODE="$?"
        if [ "${EXIT_CODE}" != 0 ] ; then
          exit "${EXIT_CODE}"
        fi
        RESULT_COMMITTER_DATE="$(git log -1 --pretty=%ct)"
        printf "%s" "${RESULT_COMMITTER_DATE}" > "$(results.committer-date.path)"
        printf "%s" "${RESULT_SHA}" > "$(results.commit.path)"
        printf "%s" "${PARAM_URL}" > "$(results.url.path)"
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: '{{ .Values.app.name }}-npm'
  labels:
    app.kubernetes.io/managed-by: jhipster-online
    app.kubernetes.io/instance: '{{ .Values.app.name }}'
    app.kubernetes.io/part-of: '{{ .Values.app.name }}'
spec:
  description: >-
    This task can be used to run npm goals on a project.

    This task can be used to run npm goals on a project where package.json is
    present and has some pre-defined npm scripts.
  params:
    - default: .
      description: The path where package.json of the project is defined.
      name: PATH_CONTEXT
      type: string
    - default:
        - version
      description: The npm goals you want to run.
      name: ARGS
      type: array
    - default: '{{ .Values.builder.image }}'
      description: The node image you want to use.
      name: IMAGE
      type: string
  steps:
    - name: npm-run
      image: $(params.IMAGE)
      computeResources: {}
      env:
        - name: CI
          value: 'true'
      workingDir: $(workspaces.source.path)/$(params.PATH_CONTEXT)
      script: |
        #!/usr/bin/env bash
        set -euo pipefail
        WS="$(workspaces.source.path)"
        PC="$(params.PATH_CONTEXT)"
        PKG="$WS/$PC/package.json"
        if [ ! -f "$PKG" ]; then
          echo "No package.json at $WS/$PC, skipping npm"
          exit 0
        fi
        cd "$WS/$PC"
        npm $(params.ARGS[*])
  workspaces:
    - name: source
---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: '{{ .Values.app.name }}'
  labels:
    app.kubernetes.io/managed-by: jhipster-online
    app.kubernetes.io/instance: '{{ .Values.app.name }}'
    app.kubernetes.io/part-of: '{{ .Values.app.name }}'
  annotations:
    chains.tekton.dev/transparency-upload: 'true'
spec:
  params:
    - default: >-
        image-registry.openshift-image-registry.svc:5000/{{ .Values.image.namespace }}/{{ .Values.app.name }}
      name: IMAGE_NAME
      type: string
    - default: '{{ .Values.tekton.pathContext }}'
      name: PATH_CONTEXT
      type: string
    - default: '{{ .Values.runtime.dotnet.sdkImage }}'
      name: DOTNET_SDK_IMAGE
      type: string
    - default: '{{ .Values.app.name }}'
      name: APP_NAME
      type: string
    - default: '{{ .Values.git.repoUrl }}'
      name: GIT_REPO
      type: string
    - default: '{{ .Values.tekton.defaultBranch }}'
      name: GIT_BRANCH
      type: string
  tasks:
    - name: dotnet
      params:
        - name: DOTNET_IMAGE
          value: $(params.DOTNET_SDK_IMAGE)
        - name: CONTEXT_DIR
          value: $(params.PATH_CONTEXT)
      runAfter:
        - npm
      taskRef:
        kind: Task
        name: '{{ .Values.app.name }}-dotnet'
      workspaces:
        - name: source
          workspace: workspace
        - name: maven-settings
          workspace: workspace
        - name: maven-local-repo
          workspace: workspace
    - name: git-clone
      params:
        - name: url
          value: $(params.GIT_REPO)
        - name: revision
          value: $(params.GIT_BRANCH)
        - name: refspec
          value: ''
        - name: submodules
          value: 'true'
        - name: depth
          value: '1'
        - name: sslVerify
          value: 'true'
        - name: crtFileName
          value: ca-bundle.crt
        - name: subdirectory
          value: ''
        - name: sparseCheckoutDirectories
          value: ''
        - name: deleteExisting
          value: 'true'
        - name: httpProxy
          value: ''
        - name: httpsProxy
          value: ''
        - name: noProxy
          value: ''
        - name: verbose
          value: 'true'
        - name: gitInitImage
          value: >-
            registry.redhat.io/openshift-pipelines/pipelines-git-init-rhel8@sha256:c4b2183f7c7997bd401d86b33eefb637b3ef2fa90618e875106292cd69a15c14
        - name: userHome
          value: /home/git
      taskRef:
        kind: Task
        name: '{{ .Values.app.name }}-git-clone'
      workspaces:
        - name: output
          workspace: workspace
    - name: npm
      params:
        - name: PATH_CONTEXT
          value: $(params.PATH_CONTEXT)
        - name: ARGS
          value:
            - install
        - name: IMAGE
          value: '{{ .Values.builder.image }}'
      runAfter:
        - oc-apply-manifest
      taskRef:
        kind: Task
        name: '{{ .Values.app.name }}-npm'
      workspaces:
        - name: source
          workspace: workspace
    - name: s2i-binary-build
      params:
        - name: SCRIPT
          value: |
            set -e
            DIR="$(workspaces.manifest-dir.path)/$(params.PATH_CONTEXT)/publish"
            test -d "$DIR"
            oc start-build {{ .Values.app.name }}-devspace --from-dir="$DIR" --follow
        - name: VERSION
          value: latest
      runAfter:
        - dotnet
      taskRef:
        kind: Task
        name: '{{ .Values.app.name }}-openshift-client'
      workspaces:
        - name: manifest-dir
          workspace: workspace
    - name: deploy
      params:
        - name: SCRIPT
          value: |
            set -e
            oc rollout restart "deployment/$(params.APP_NAME)"
            oc rollout status "deployment/$(params.APP_NAME)" --timeout=15m
        - name: VERSION
          value: latest
      runAfter:
        - s2i-binary-build
      taskRef:
        kind: Task
        name: '{{ .Values.app.name }}-openshift-client'
      workspaces:
        - name: manifest-dir
          workspace: workspace
    - name: oc-apply-manifest
      params:
        - name: SCRIPT
          value: >-
            echo 'Skipping legacy template.yaml apply; use JHipster Online Fabric8/Argo CD deploy for helm/ manifests.'
        - name: VERSION
          value: latest
      runAfter:
        - git-clone
      taskRef:
        kind: Task
        name: '{{ .Values.app.name }}-openshift-client'
      workspaces:
        - name: manifest-dir
          workspace: workspace
  workspaces:
    - name: workspace
      optional: false
  finally: []
---
apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  generateName: '{{ .Values.app.name }}-'
  labels:
    tekton.dev/pipeline: '{{ .Values.app.name }}'
    app.kubernetes.io/managed-by: jhipster-online
    app.kubernetes.io/instance: '{{ .Values.app.name }}'
    app.kubernetes.io/part-of: '{{ .Values.app.name }}'
spec:
  params:
    - name: IMAGE_NAME
      value: 'image-registry.openshift-image-registry.svc:5000/{{ .Values.image.namespace }}/{{ .Values.app.name }}'
    - name: PATH_CONTEXT
      value: '{{ .Values.tekton.pathContext }}'
    - name: DOTNET_SDK_IMAGE
      value: '{{ .Values.runtime.dotnet.sdkImage }}'
    - name: APP_NAME
      value: '{{ .Values.app.name }}'
    - name: GIT_REPO
      value: '{{ .Values.git.repoUrl }}'
    - name: GIT_BRANCH
      value: '{{ .Values.tekton.defaultBranch }}'
  pipelineRef:
    name: '{{ .Values.app.name }}'
  taskRunTemplate:
    serviceAccountName: pipeline
  timeouts:
    pipeline: '{{ .Values.tekton.timeout }}'
  workspaces:
    - name: workspace
      persistentVolumeClaim:
        claimName: '{{ .Values.app.name }}-workspace'
