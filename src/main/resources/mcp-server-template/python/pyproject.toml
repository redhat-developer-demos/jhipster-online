[project]
name = "__PROJECT_NAME__"
version = "0.1.0"
description = "MCP server (Python) generated by JHipster Online"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "mcp>=1.27.0",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
